name: Pages

on:
  repository_dispatch:
    types: [trigger-deploy]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.DEPLOY_KEY }}
          # If your repository depends on submodule, please see: https://github.com/actions/checkout
          submodules: recursive
          fetch-depth: 0 

      - name: Set Git Config
        run: |
          git config --global user.email "xu.baicheng@foxmail.com"
          git config --global user.name "Channing Hsu"

      - name: Remove and re-add submodule
        run: |
          # 移除子模块
          git submodule deinit -f -- source/_posts
          rm -rf .git/modules/source/_posts
          git rm -f source/_posts
          
          # 重新添加子模块
          git submodule add -b main https://github.com/channinghsu/Blog-Post.git source/_posts
          
          # 更新和初始化子模块
          git submodule update --init --recursive
          
          # 提交更改
          git add .gitmodules source/_posts
          git commit -m "Re-add source/_posts submodule pointing to main branch" || echo "No changes to commit"
          
          # 推送更改
          git push

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Cache NPM dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.OS }}-npm-cache
          restore-keys: |
            ${{ runner.OS }}-npm-cache

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public
  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
